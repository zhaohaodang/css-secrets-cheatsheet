---
sidebar_position: 1
---

```css
div {
  text-align: justify;
  hyphens: auto;
}
```

<div
  style={{
    width: 135,
    textAlign: "justify",
    hyphens: "auto",
  }}
>
  The only way to get rid of a temptation is to yield to it.
</div>

:::warning
你需要为 HTML 标签的 lang 属性指定合适的语言，比如 en，否则可能看不到效果。
:::
